version: 2

jobs:
  build:
    filters:
      branches:
        ignore: gh-pages
    shell: /bin/bash --login
    docker:
      - image: circleci/node:12-browsers
    working_directory: ~/sharp-dance
    steps:
      - checkout
      - run: ./.circleci/ci.sh
